# Project-specific ProGuard rules.
# Configure applied rule files through the proguardFiles setting in build.gradle.

# Add keep rules here only for project code that requires preservation during
# shrinking or obfuscation (for example, reflection or JavaScript interfaces).
